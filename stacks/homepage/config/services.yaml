# Homepage — service groups (https://gethomepage.dev/configs/services/).
#
# The front-door list. `whoami` is the live Phase-3 proof; real apps (Jellyfin,
# Immich, …) are added as they land in later phases. Every href is the app's
# friendly HTTPS name, resolved by AdGuard to the Dell and served by Traefik under
# the shared wildcard cert.

- Infrastructure:
    - Komodo:
        icon: komodo.png
        href: http://10.0.0.70:9120
        description: Fleet orchestration (Core) — LAN/Tailscale only
    - AdGuard Home:
        icon: adguard-home.png
        href: http://10.0.0.70:3000
        description: Internal DNS + ad-blocking — LAN/Tailscale only
    - wg-easy:
        icon: wireguard.png
        href: http://10.0.0.70:51821
        description: Family/friends VPN admin — LAN/Tailscale only

- Apps:
    - whoami:
        icon: traefik.png
        href: https://whoami.ragnaforge.xyz
        description: Phase-3 edge proof (traefik/whoami)
